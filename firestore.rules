rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ログインした本人だけが、自分のお店リストを読んだり書いたりできる
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
